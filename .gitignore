# Dependencies
node_modules/

# Logs
npm-debug.log*
*.log

# Environment (none should ever exist here, but never commit one)
.env
.env.*

# OS junk
.DS_Store
Thumbs.db

# Claude Code / loop-engineering meta — local tooling, not site content
# (Express serves the repo root, so anything committed becomes web-accessible)
.claude/
CLAUDE.md
MEMORY.md

# Local tool caches & scratch
.npm-cache/
.tools/
.agents/
.codex/
.edge-qa*/
*.tmp
qa-tmp-*
*-qa.png
recolor-*.png
liquid-glass-*.png

# Raw video sources — only the encoded pipeline outputs ship
# (raw exports are HEVC/oversized masters; encode first, commit the output)
videos/logoLoop.mp4
videos/cube4k*.mp4
videos/blueprint-cube-hero.mp4
videos/blueprint-cube-hero-*
